/**
 * Created with JetBrains WebStorm.
 * User: loman_000
 * Date: 10.09.13
 * Time: 22:38
 * To change this template use File | Settings | File Templates.
 */

import engine = module('engine');

function ShowField(canvas_control:CanvasRenderingContext2D, field:engine.Field):void
{
      for(var y = 0; y < field.height; y++)
      {
          for( var x = 0; x < field.width; x++ )
          {

          }
      }
}